# Lessons

- 首页设置页的“保存”动作只负责应用并持久化设置，不能顺带触发首页自动重新加载或重新生成文章。首页刷新只能由用户显式触发，例如下拉加载或点击刷新按钮。
- 当需求是“前置某个菜单项”时，优先做插入或重排，不能用整体替换系统菜单的方式实现，否则容易把用户仍然需要的系统动作一起删掉。
- 在 `List` 里做展开/折叠动画时，优先保留稳定的子视图结构，通过 `lineLimit` 或显隐状态切换内容，避免条件插入/移除导致整行跟着抖动。
